# ============================================
# CONFIGURACIÓN DEL BACKEND - MINISTERIO AMVA
# ============================================
# Renombra este archivo a .env y configura los valores

# --------------------------------------------
# BASE DE DATOS (PostgreSQL)
# --------------------------------------------
# Local:
# DATABASE_URL="postgresql://postgres:password@localhost:5432/ministerio_amva"
# 
# Railway (se genera automáticamente):
# DATABASE_URL="postgresql://user:pass@host.railway.app:5432/railway"
DATABASE_URL="postgresql://postgres:password@localhost:5432/ministerio_amva"

# --------------------------------------------
# JWT (Autenticación)
# --------------------------------------------
# IMPORTANTE: Usa una clave larga y segura en producción
# Puedes generar una con: openssl rand -base64 32
JWT_SECRET="tu_clave_secreta_super_segura_cambiar_en_produccion"
JWT_EXPIRES_IN="7d"

# --------------------------------------------
# CLOUDINARY (Imágenes y Videos)
# --------------------------------------------
# Obtén estas credenciales en: https://cloudinary.com/console
CLOUDINARY_CLOUD_NAME="tu_cloud_name"
CLOUDINARY_API_KEY="tu_api_key"
CLOUDINARY_API_SECRET="tu_api_secret"

# --------------------------------------------
# CORS (Seguridad)
# --------------------------------------------
# URL del frontend (Vercel)
# Local: http://localhost:3000
# Producción: https://tu-dominio.com
FRONTEND_URL="http://localhost:3000"

# --------------------------------------------
# SERVIDOR
# --------------------------------------------
# Railway asigna el puerto automáticamente via variable PORT
PORT=4000
NODE_ENV="development"

# --------------------------------------------
# REDIS (Cola de Notificaciones - Bull)
# --------------------------------------------
# Local:
# REDIS_HOST="localhost"
# REDIS_PORT="6379"
# REDIS_PASSWORD=""
# REDIS_DB="0"
# 
# Railway/Producción (se genera automáticamente):
# REDIS_HOST="redis.railway.app"
# REDIS_PORT="6379"
# REDIS_PASSWORD="tu_password"
# REDIS_DB="0"
REDIS_HOST="localhost"
REDIS_PORT="6379"
REDIS_PASSWORD=""
REDIS_DB="0"

# --------------------------------------------
# EMAIL (Configuración de Email)
# --------------------------------------------
# IMPORTANTE: Gmail SMTP puede bloquear conexiones desde servicios cloud (Render, Railway, etc.)
# RECOMENDADO PARA PRODUCCIÓN: Usar SendGrid o Resend en lugar de Gmail SMTP
# Ver documentación completa en: docs/CONFIGURACION_EMAIL_PRODUCCION.md

# Proveedor de email a usar: 'sendgrid', 'resend', 'gmail', o 'smtp'
# Por defecto: 'gmail' (solo para desarrollo local)
EMAIL_PROVIDER="gmail"

# --------------------------------------------
# SendGrid (Recomendado para producción)
# --------------------------------------------
# Plan gratuito: 100 emails/día
# Planes de pago desde $15/mes para 40,000 emails
# 1. Crea cuenta en https://sendgrid.com
# 2. Verifica un email en Settings → Sender Authentication
# 3. Crea API Key en Settings → API Keys
# 4. Configura estas variables:
# SENDGRID_API_KEY="SG.xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# SENDGRID_FROM_EMAIL="noreply@tudominio.com"  # Debe estar verificado en SendGrid
# SENDGRID_FROM_NAME="AMVA Digital"

# --------------------------------------------
# Resend (Recomendado si tienes dominio propio)
# --------------------------------------------
# Plan gratuito: 3,000 emails/mes
# Planes de pago desde $20/mes para 50,000 emails
# 1. Crea cuenta en https://resend.com
# 2. Verifica dominio o email en Domains/Emails
# 3. Crea API Key en API Keys
# 4. Configura estas variables:
# RESEND_API_KEY="re_xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx"
# RESEND_FROM_EMAIL="noreply@tudominio.com"  # Debe estar verificado en Resend
# RESEND_FROM_NAME="AMVA Digital"

# --------------------------------------------
# Gmail SMTP (Solo para desarrollo local)
# --------------------------------------------
# ⚠️ NO RECOMENDADO para producción en servicios cloud
# Para Gmail, necesitas una App Password: https://myaccount.google.com/apppasswords
# Gmail puede bloquear conexiones desde Render, Railway, etc.
SMTP_HOST="smtp.gmail.com"
SMTP_PORT="587"
SMTP_SECURE="false"
SMTP_USER="tu_email@gmail.com"
SMTP_PASSWORD="tu_app_password"


